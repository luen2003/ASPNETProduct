@{
    ViewData["Title"] = "Lỗi hệ thống";
}

<h1 class="text-danger">Đã xảy ra lỗi</h1>
<p>Xin lỗi, có lỗi xảy ra trong quá trình xử lý yêu cầu của bạn.</p>

@if (Model != null)
{
    <div>
        <strong>Chi tiết lỗi:</strong>
        <pre>@Model</pre>
    </div>
}
